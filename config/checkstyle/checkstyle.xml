<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Configuracao de estilo do MeuPet.
  Mantem as convencoes do projeto: indentacao com tabs, chaves na mesma linha
  e nomes no padrao Java (permite underlines nos nomes de teste).
-->
<module name="Checker">
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- Linhas muito longas dificultam a leitura. -->
    <module name="LineLength">
        <property name="max" value="140"/>
    </module>

    <module name="TreeWalker">
        <!-- Imports sempre no topo, sem repeticao. -->
        <module name="UnusedImports"/>
        <module name="RedundantImport"/>

        <!-- Chaves na mesma linha (estilo K&R). -->
        <module name="NeedBraces">
            <property name="allowSingleLineStatement" value="false"/>
        </module>
        <module name="LeftCurly">
            <property name="option" value="eol"/>
        </module>
        <module name="RightCurly">
            <property name="option" value="same"/>
        </module>

        <!-- Espacos ao redor de operadores. -->
        <module name="WhitespaceAround"/>
        <module name="ParenPad"/>
        <module name="TypecastParenPad"/>

        <!-- Convencoes de nomenclatura. -->
        <module name="TypeName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
        <module name="ConstantName"/>
        <module name="MethodName">
            <property name="format" value="^[a-z][a-zA-Z0-9_]*$"/>
        </module>

        <!-- Impede modificadores como 'public abstract' em classes abstratas. -->
        <module name="RedundantModifier"/>

        <!-- Nao deixar catch vazio. -->
        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="expected"/>
        </module>

        <!-- Sem espaços apos certos tokens. -->
        <module name="NoWhitespaceAfter"/>
        <module name="NoWhitespaceBefore">
            <property name="allowLineBreaks" value="true"/>
        </module>
    </module>
</module>